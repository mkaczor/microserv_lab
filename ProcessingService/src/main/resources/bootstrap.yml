spring:
  application:
    name: ProcessingService

server:
  port: 8804

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8702/eureka, http://localhost:8701/eureka


