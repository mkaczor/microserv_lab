spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mkaczor/microserv_lab_conf
          force-pull: true

server:
  port: 8600


