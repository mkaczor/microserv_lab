spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: file://C:/Materials/JVA-043/config-repo
          force-pull: true

server:
  port: 8600


